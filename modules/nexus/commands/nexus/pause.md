---
name: pause
description: Deprecated — use /nexus:progress --pause
argument-hint: "[reason]"
allowed-tools: [Read, Write]
---

# nexus:pause — Deprecated

This command is deprecated. Use `/nexus:progress --pause` instead.

`/nexus:progress` is the universal session command — it handles orient, resume, AND pause.

**Do this instead:**

```
/nexus:progress --pause [optional reason]
```

Redirecting to `/nexus:progress --pause`...
